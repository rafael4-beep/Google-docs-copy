// Client-safe constants (no Node-only imports) shared across components.

export const SUPPORTED_LABEL =
  "You can upload .txt, .md and .docx files (max 5MB). Each upload becomes a new editable document.";
